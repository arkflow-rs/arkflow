# Tasks: add-sql-transactional-batch

- [x] 1.1 `execute_insert_transactional`（双方言）+ `write_batch` 覆写。
- [x] 1.2 既有 sql output 测试回归（10 个）。
- [x] 1.3 spec 同步：sql-output（事务原子）、exactly-once-output（L3 延后理由）。
