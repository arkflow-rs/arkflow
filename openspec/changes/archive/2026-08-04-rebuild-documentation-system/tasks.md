## 1. Baseline and content contract

- [x] 1.1 Inventory current docs, versioned routes, README entry points, existing CI checks, and known broken links; record the migration map in the change notes.
- [x] 1.2 Add contributor guidance defining page types, metadata, headings, examples, compatibility notices, ownership, and review expectations.
- [x] 1.3 Define the canonical component/configuration metadata shape and document its ownership and update workflow.

## 2. Information architecture and navigation

- [x] 2.1 Create stable landing pages for Start Here, Build Pipelines, Concepts, Components, Configure, Operate, Reference, and Contribute, each stating audience and next steps.
- [x] 2.2 Replace the autogenerated sidebar with explicit curated groups and verify every maintained page is reachable from an intentional navigation path.
- [x] 2.3 Move or wrap existing high-value pages into the new taxonomy and add redirects/compatibility stubs for changed routes.
- [x] 2.4 Update Docusaurus navbar/footer, root READMEs, and version navigation to point at the new canonical entry points.

## 3. Canonical references and examples

- [x] 3.1 Implement the component/configuration inventory and extraction/validation adapter against the supported ArkFlow registry or declared overlay data.
- [x] 3.2 Normalize component reference pages and add deterministic generated sections for supported names, configuration fields, and example links.
- [x] 3.3 Add or refresh representative end-to-end examples for local quickstart, stream configuration, control-plane operations, durability, and common input/output paths.
- [x] 3.4 Add compatibility and deprecation notices to version-sensitive reference and operations pages.

## 4. Quality gates and CI

- [x] 4.1 Add the repository-local `docs:check` command and checks for metadata, headings, sidebar reachability, internal links, anchors, and explicit exceptions.
- [x] 4.2 Add example/configuration validation, component coverage checks, and deterministic generated-reference verification with actionable file/line errors.
- [x] 4.3 Add a CI workflow/job that runs the same local check command and the production Docusaurus build on documentation changes.
- [x] 4.4 Clean up existing baseline violations, then make documentation validation and build status required/release-blocking according to repository policy.

## 5. Versioning and release lifecycle

- [x] 5.1 Document current/supported version policy, snapshot creation, archival, compatibility labeling, redirects, and rollback procedure.
- [x] 5.2 Add a release documentation checklist covering generated references, examples, links, version metadata, build output, and audit artifacts.
- [x] 5.3 Validate current and supported versioned builds and publish the migration map for renamed routes.

## 6. Verification and handoff

- [x] 6.1 Run formatting/lint checks for scripts and TypeScript, `docs:check`, and the production docs build from a clean dependency lockfile.
- [x] 6.2 Review navigation and key journeys locally (new user, pipeline author, operator, contributor) and fix dead ends or misleading labels.
- [x] 6.3 Run `openspec validate "rebuild-documentation-system" --strict` and record known non-blocking warnings with owners and follow-up tasks.
